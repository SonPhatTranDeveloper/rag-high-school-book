- Bước 3. Di chuyển con trỏ vào khoảng trắng trong khung Hiển thị danh sách đối tượng, sau đó nhấp chuột phải, lựa chọn mục Các đối tượng phụ.

[Hình ảnh minh họa khung "Hiển thị danh sách đối tượng" với các mục như Hình tứ giác, Hình đa giác, Prism và Đoạn thẳng cùng các giá trị tương ứng]

- Bước 4. Di chuyển con trỏ vào mục Hình đa giác trong khung Hiển thị danh sách đối tượng, sau đó nhấp chuột phải và lựa chọn mục Hiển thị đối tượng để ẩn 3 hình vuông đã dựng.

[Hình ảnh minh họa phần "Hình đa giác" với tùy chọn "Hiển thị đối tượng"]

Làm tương tự Bước 4 cho đối tượng Đoạn thẳng và đối tượng Hình tứ giác để ẩn tất cả các đoạn thẳng và tứ giác.

- Bước 5. Trong khung Hiển thị danh sách đối tượng, di chuyển con trỏ đến ô tròn màu trắng của mục Mat4, sau đó nhấp chuột để hiển thị Mat4.

Tiếp tục nhấp chuột chọn Mat4, di chuyển con trỏ đến biểu tượng Thiết lập màu và sự trong suốt để tô màu cho tứ giác (ở đây ta tô màu đỏ và kéo thanh trượt để ở độ đậm lớn nhất).

[Hình ảnh minh họa khung "Hiển thị danh sách đối tượng" với các mục Mat1 đến Mat7 và khung "Vùng làm việc" với bảng màu]

Làm tương tự Bước 5 cho các đối tượng:
Mat12 (ở đây ta tô màu đỏ);
Mat7, Mat11 (ở đây ta tô hai mặt này màu xanh da trời);
Mat2, q1 (ở đây ta tô hai mặt này màu xanh lá cây).

87